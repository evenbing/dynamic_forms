@using Orchard.Utility.Extensions

@{
    string name = Model.Name;
    //string isEventClass = Model.IsEvent ? "event" : null;
    //string canStartClass = Model.CanStartWorkflow ? "canStart" : null;
}

<div class="dynamicfield field-@name.HtmlClassify()" data-ClientID="">
    <div class="dynamicfield-delete" data-clientid="" style="display: none">
        <label title="Remove"></label>
    </div>
    <div>@name.CamelFriendly()</div>
</div>

